# Third-party notices

R2Beam downloads and packages these browser-side components during `npm run build`:

- `@ffmpeg/ffmpeg` 0.12.15 — MIT
- `@ffmpeg/core` 0.12.10 — GPL-2.0-or-later

The generated files under `public/vendor/ffmpeg/` are not committed. Anyone who
redistributes a built copy must comply with the applicable third-party licenses,
including the GNU GPL terms for `@ffmpeg/core`.

Dependency license metadata and source links are available from each package's
npm distribution.
